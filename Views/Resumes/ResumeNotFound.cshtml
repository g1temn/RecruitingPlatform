@{
    ViewData["Title"] = "Couldn't find resume";
}

<div class="flex-grow flex flex-col items-center justify-center gap-4">
    <h1 class="text-2xl text-brand-dark font-poppins font-bold">Couldn't find the resume!</h1>
    <a asp-controller="Resumes" asp-action="Index" class="text-sm text-gray-400 font-inter">Back to all the resumes</a>
</div>